# ----------------------------------------------------------------------
# Minimal requirements for CNN-JEPA pretraining (train_ijepacnn.py)
# ----------------------------------------------------------------------

torch>=1.10.0          # or whichever PyTorch version you prefer
torchvision>=0.11.0    # matching torchvision to your torch version

# PyTorch Lightning (the training loop is built on Lightning)
pytorch-lightning>=1.8.0

# Hydra for config management
hydra-core>=1.3.0

# timm is used for many model backbones like ResNet, ConvNeXt, etc.
timm>=0.9.2

# lightly is used by CNN-JEPA for efficient data loading & masking
lightly>=1.4.0

# Common dependencies
numpy>=1.19.0
pandas>=1.1.0
pyyaml>=5.4.0
scikit-learn>=1.0.0
scikit-image>=0.19.0
einops>=0.6.0

# (Optional) If you want WandB logging
wandb>=0.15.0

# (Optional) If your config or scripts import these
torchmetrics>=0.10.0
omegaconf>=2.3.0
